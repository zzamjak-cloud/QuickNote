# Session Start Checklist

- Activate project: `D:/0_Client/QuickNote`.
- Read `mem:project_keywords` before broad QuickNote code work.
- Run `git status --short --branch` before edits.
- For bug regressions, find the existing working analogue first, then add a targeted regression test.
- For infra/schema/resolver changes, run infra tests/build and deploy CDK before push when requested.
