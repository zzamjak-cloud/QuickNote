Phase 1 — 자산 관리 인프라 + UI

  CDK / 백엔드
  1. AssetUsage DDB 테이블 신설 (ownerId / assetId / pageId / blockId)
  2. AppSync 스키마: listMyAssets, getAssetUsages, deleteMyAssets
  3. v5-resolvers/handlers/asset.ts 신규
  4. updatePage / createPage / deletePage 핸들러에 ref-sync 훅
  5. extractAssetRefs(contentJson) 헬퍼 (quicknote-image://, quicknote-file://)
  6. 마이그레이션 Lambda — 모든 Page scan → AssetUsage seed (idempotent, 1회 invoke)

  프론트
  1. Settings → "자산 관리" 탭 (AdminAssetsTab.tsx)
  2. 가상 스크롤 표 (썸네일 / 이름 / MIME / 크기 desc 기본 / 사용 페이지 수)
  3. 필터: 사용 안 됨 / 크기 ≥ N / MIME
  4. 행 클릭 → 상세 패널 (미리보기 + 사용 페이지 리스트 + 액션)
  5. 다중 선택 + 영구 삭제 (확인 모달, 휴지통 비우기 패턴 재사용)
  1. Settings → "자산 관리" 탭 (AdminAssetsTab.tsx)
  2. 가상 스크롤 표 (썸네일 / 이름 / MIME / 크기 desc 기본 / 사용 페이지 수)
  3. 필터: 사용 안 됨 / 크기 ≥ N / MIME
  4. 행 클릭 → 상세 패널 (미리보기 + 사용 페이지 리스트 + 액션)
  5. 다중 선택 + 영구 삭제 (확인 모달, 휴지통 비우기 패턴 재사용)

  Phase 2 — 사용자 트리거 압축/교체

  - @ffmpeg/* 재설치 (lazy load — 액션 버튼 클릭 시점에만)
  - 3가지 액션:
    - GIF → MP4 변환 (ffmpeg)
    - MP4 재압축 (CRF 28 medium, ffmpeg)
    - JPG/PNG 압축 (브라우저 Canvas — ffmpeg 불필요)
  - 공통 흐름: 새 자산 업로드 → 서버 사이드 replaceAssetRef mutation 으로 페이지 content 치환 +
  AssetUsage 갱신 → 옛 자산 영구 삭제

  ImageAsset 스키마 확인 결과

  - ownerId 기반 (workspaceId 아님) → 자산 관리는 사용자 단위
  - name 필드 없음 → 업로드 시점에 name 컬럼 추가 (UI 표시용)

  진행 순서

  지금 Phase 1 부터 시작합니다. 큰 작업이라 다음 체크포인트마다 짧게 보고:

  1. 체크포인트 A: CDK + GraphQL 스키마 추가 (배포 대상 변경)
  2. 체크포인트 B: 백엔드 핸들러 + 마이그레이션 Lambda
  3. 체크포인트 C: 프론트 UI